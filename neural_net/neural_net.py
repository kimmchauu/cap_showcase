import numpy as np 
import math


class NeuralNet:
    """A simple neural network. 
    """
    def __init__(self, model_name, n_layers, n_features, n_nodes_hl): 
        """
        Args:
            model_name (string): name of model
            n_layers (int): number of hidden layers
            n_features (int): number of features from input
            n_nodes_hl (int): Number of nodes in hidden layers
        """
        self.model_name = model_name
        self.n_layers = n_layers
        self.n_features = n_features
        self.n_nodes_hl = n_nodes_hl
        
    def input_layer(self):

    def output_layer(self):
        nn_out = 1

    def create_network(self):

        #Creating theta matrices for movment between layers as values inside a dictionary. 
        
    def train_model(self):
    
    
    
    
    
    